# Jekyll Configuration
source: .
destination: _site
exclude:
  - .vscode
  - README.md
  - .gitignore
  - Gemfile
  - Gemfile.lock

# Disable SCSS processing if not needed
sass:
  compile: false

# GitHub Pages specific settings
plugins:
  - jekyll-github-metadata
  - jekyll-seo-tag
